# ===========================================
# 本地开发配置模板
# 使用方法：复制此文件为 application-local.yml，填入真实 Key
#   cp application-local.yml.example application-local.yml
#
# application-local.yml 已在 .gitignore 中，不会被提交
# ===========================================

ai:
  deepseek:
    api-key: sk-你的真实key填在这里
